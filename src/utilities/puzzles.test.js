import {
  parsePuzzles,
} from './puzzles.js';

describe('utilities/puzzles', () => {
  describe('parsePuzzles', () => {
    it('is a function', () => {
      expect(typeof parsePuzzles).toBe('function');
    });
  });
});
